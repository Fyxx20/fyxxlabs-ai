# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_PRO_MONTHLY=price_...
STRIPE_PRICE_PRO_YEARLY=price_...

# OpenAI (server-only, never expose to client)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# OPENAI_TEMPERATURE=0.2
# OPENAI_MAX_OUTPUT_TOKENS=1200

# App (optional)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Admin : emails ayant accès au back-office même si profiles.role n'est pas "admin" (séparés par des virgules)
# ADMIN_EMAILS=admin@storepilot.ia,m.harea@storepilot.ia
# Super admin prioritaire (accès total + protections renforcées)
# SUPER_ADMIN_EMAILS=owner@fyxxlabs.com

# Connectors (intégrations plateforme)
# Clé 32 bytes (hex ou 32 caractères) pour chiffrer les credentials en base
INTEGRATIONS_ENC_KEY=your-32-char-or-hex-encryption-key!!

# Shopify OAuth (optionnel)
# SHOPIFY_CLIENT_ID=...
# SHOPIFY_CLIENT_SECRET=...
# SHOPIFY_OAUTH_REDIRECT_URI=http://localhost:3000/api/integrations/shopify/callback

# WooCommerce / PrestaShop
# Pas d'OAuth natif ici: les identifiants se saisissent dans l'UI (Gerer la boutique > Connexions plateforme).
# WooCommerce: Consumer Key + Consumer Secret (REST API en read)
# PrestaShop: API Key (Webservice active)

# Optional: Playwright browsers path if needed
# PLAYWRIGHT_BROWSERS_PATH=/path/to/browsers

# Optional: Cron secret if using cron jobs
# CRON_SECRET=your-cron-secret
